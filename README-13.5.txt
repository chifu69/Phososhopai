PHOTO IA 13.5 — Selection IA Stable

Built directly from the uploaded PHOTO IA 13.3 package.

Fixes:
- New unique 13.5 cache so iPhone cannot keep loading 13.4/13.4.1 code.
- GitHub Pages navigation is network-first, preventing stale index.html after deployments.
- Selection IA work canvas is smaller on iPhone for better responsiveness.
- Mask rendering now has a hard timeout so the app cannot remain on the spinning processing overlay forever.
- Extra UI yields were added around the local selection work.
- All files remain flat at the ZIP root; no folders are needed.
